{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default local desktop capability for the main host window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
